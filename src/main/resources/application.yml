spring:
  application:
    name: gameBoard

  profiles:
    active: local

  jpa:
    hibernate:
      ddl-auto: update   # ??? ?? ? DB ??? ?? ??
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true

    server:
      port: 8080